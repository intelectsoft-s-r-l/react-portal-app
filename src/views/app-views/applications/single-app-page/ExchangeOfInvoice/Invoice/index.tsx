import * as React from 'react'
import { RouteComponentProps } from 'react-router-dom';

const Invoice = ({ match, location, history }: RouteComponentProps) => {
    return (<div>Invoice</div>);
}
export default Invoice;