import * as React from 'react'
import { RouteComponentProps } from 'react-router-dom';

const Order = ({ match, location, history }: RouteComponentProps) => {
    return (
        <div>Order</div>
    );
}
export default Order;